#include <iostream>
using namespace std;
void dfs(int node , vector<int>&dfsTraversal ,vector<int>adj[] , vector<int>&visi)
{
    visi[node]=1;
    dfsTraversal.push_back(node);
    for(auto it : adj[node])
    {
        if(visi[it] == 0)
        {
            dfs(it,dfsTraversal, adj , visi);
        }
    }
}
vector<int> connectedComponents(int V , vector<int>adj[])
{
    vector<int>visi(V,0);
    int start =0;
    vector<int>dfsTraversal;
    dfs(0,V,adj,vis);
    return dfsTraversal;
}
