#include <bits/stdc++.h>
using namespace std;

//Memoization
int f(int ind , vector<int>&heights , vector<int>&dp)
{
    if(ind == 0)return 0;
    if (dp[ind] != -1) return dp[ind];
    int jumps;
    int minSteps=INT_MAX;
    for(int j=1;j<=k;j++)
    {
        if(ind-j >= 0)
        {
            jumps = f(ind-j,heights,dp)+abs(heights[ind]-heights[ind-j]);
            minSteps=min(minSteps,jumps); 

        }
    }
    return dp[ind]=minSteps;
}
int frogJumpK(int n, vector<int> &heights) {

    vector<int> dp(n + 1, -1);

    return f(n - 1, heights, dp);
}

//Tabulation
int frog(int n ,vector<int>&heights)
{
    vector<int>dp(n,-1);
    dp[0]=0;

    for(int i=1;i<n;i++)
    {
        int minSteps=INT_MAX;
        for(int j=1;j<=k;j++){
        if(ind-j >= 0)
        {
            jumps = f(i-j,heights)+abs(heights[i]-heights[i-j]);
            minSteps=min(minSteps,jumps); 
        }
        dp[i]=minSteps;

    }
}
return dp[n-1];
}
